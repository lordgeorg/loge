diff --git a/CMakeLists.txt b/CMakeLists.txt
index d9642ad..232d357 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -128,8 +128,8 @@ if((UNIX AND SYSTEM_DIR_INSTALL) OR OPTION_FORCE_SYSTEM_DIR_INSTALL)
     set(INSTALL_EXAMPLES  "share/${project}")       # /usr/[local]/share/<project>
     set(INSTALL_DATA      "share/${project}")       # /usr/[local]/share/<project>
     set(INSTALL_BIN       "bin")                    # /usr/[local]/bin
-    set(INSTALL_SHARED    "lib")                    # /usr/[local]/lib
-    set(INSTALL_LIB       "lib")                    # /usr/[local]/lib
+    set(INSTALL_SHARED    "lib64")                    # /usr/[local]/lib
+    set(INSTALL_LIB       "lib64")                    # /usr/[local]/lib
     set(INSTALL_INCLUDE   "include")                # /usr/[local]/include
     set(INSTALL_DOC       "share/doc/${project}")   # /usr/[local]/share/doc/<project>
     set(INSTALL_SHORTCUTS "share/applications")     # /usr/[local]/share/applications
@@ -142,8 +142,8 @@ else()
     set(INSTALL_EXAMPLES  ".")                      # ./
     set(INSTALL_DATA      ".")                      # ./
     set(INSTALL_BIN       ".")                      # ./
-    set(INSTALL_SHARED    "lib")                    # ./lib
-    set(INSTALL_LIB       "lib")                    # ./lib
+    set(INSTALL_SHARED    "lib64")                    # ./lib
+    set(INSTALL_LIB       "lib64")                    # ./lib
     set(INSTALL_INCLUDE   "include")                # ./include
     set(INSTALL_DOC       "doc")                    # ./doc
     set(INSTALL_SHORTCUTS "misc")                   # ./misc
